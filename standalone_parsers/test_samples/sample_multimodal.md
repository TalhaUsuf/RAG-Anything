# RAG-Anything Multimodal Test Document

## Introduction

This is a test document that contains text mixed with image references.
The parser should extract images separately and send them to the Vision LLM.

## Test Image 1

Below is a test image generated by PIL:

![RAG-Anything Test Image](sample_image.png)

The image above shows text rendered on a white background with colored labels.

## Test Image 2

Here is the BMP format test image:

![BMP Format Test](sample_image.bmp)

This image was created to verify format conversion works correctly.

## Architecture Overview

RAG-Anything uses a mixin-based architecture:
- QueryMixin for text and multimodal queries
- ProcessorMixin for document parsing and processing
- BatchMixin for folder-level batch operations

## Conclusion

Both images above should be extracted as multimodal chunks with
Vision LLM descriptions, while the text content flows through the
normal MinerU pipeline.
